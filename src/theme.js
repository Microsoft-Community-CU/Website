import {createMuiTheme} from '@material-ui/core'

const theme = createMuiTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#0078d4',
        },
        secondary: {
            main: '#5c2d91'
        }
    }
})

export default theme